const PageTitle = ({ pageTitle }) => {
  return (
    <div className="py-12 cursive--font bg-black text-white text-center md:text-5xl text-3xl font-semibold ">
      {pageTitle}
    </div>
  );
};

export default PageTitle;
